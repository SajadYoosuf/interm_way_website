# Set this to the final production origin so canonical, Open Graph, robots and
# sitemap URLs point to the public domain. Do not include a trailing slash.
NEXT_PUBLIC_SITE_URL=https://www.example.com
